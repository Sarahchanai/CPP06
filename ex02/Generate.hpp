#ifndef		GENERATE_HPP
# define	GENERATE_HPP
# include	"Base.hpp"

// cf sujet : "Base * generate(void) It randomly instantiates A, B, or C and returns the instance as a Base pointer");
Base*	generate(void);

#endif