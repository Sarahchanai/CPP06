#ifndef DATA_HPP
#define DATA_HPP


#include <string>



//NB : data = le colis que le livreur doit finalt remettre;
// NB : struct data, cf sujet "You must create a non-empty (meaning it has data members) Data structure"


struct Data
{
	std::string	name; // = contenu du colis
	int	id; // = numero de suivi
};






#endif